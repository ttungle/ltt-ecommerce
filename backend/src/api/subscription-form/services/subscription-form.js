'use strict';

/**
 * subscription-form service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::subscription-form.subscription-form');
